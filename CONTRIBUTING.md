# 贡献指南

修改核心前先确认问题不能在任务配置或适配层解决。core 只能依赖标准库和自己的契约；不要添加物体、机器人或供应商名称分支。

新增适配器需要说明输入能力、执行预算含义、观测时间域、停止行为和 context 回执。增加一个无需账号或硬件的契约测试，并说明尚未验证的真实能力。

Python 最低 3.10。代码注释使用英文，用户文档使用中文。提交前运行 README 中的测试、lint 与格式检查。不要提交模型权重、账号信息或实验相机数据。

贡献的原创代码按本目录 Apache-2.0 许可提供。引入第三方代码时保留其许可和署名；父项目文件不自动获得本包许可。
